<Window x:Class="E3Studio.Dialogs.MaterialLibraryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Material Library Management"
        Height="520" Width="720"
        Background="{StaticResource BgCardBrush}"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="36" CornerRadius="12" GlassFrameThickness="0" ResizeBorderThickness="0"/>
    </WindowChrome.WindowChrome>
    
    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>  <!-- Title -->
                <RowDefinition Height="*"/>   <!-- Content -->
                <RowDefinition Height="60"/>  <!-- Buttons -->
            </Grid.RowDefinitions>
            
            <!-- Title Bar -->
            <Border Grid.Row="0" Background="{StaticResource BgCardBrush}" CornerRadius="12,12,0,0">
                <Grid>
                    <TextBlock Text="Material Library Management" 
                               Foreground="{StaticResource TextBrush}" 
                               FontWeight="SemiBold"
                               VerticalAlignment="Center" 
                               Margin="16,0"/>
                    
                    <Button HorizontalAlignment="Right" 
                            Width="36" Height="36"
                            WindowChrome.IsHitTestVisibleInChrome="True"
                            Click="Close_Click"
                            Cursor="Hand"
                            Background="Transparent" 
                            BorderThickness="0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="0,12,0,0">
                                                <Path x:Name="icon" Data="M8,8 L16,16 M16,8 L8,16" Stroke="{StaticResource TextMidBrush}" StrokeThickness="1.5"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bg" Property="Background" Value="{StaticResource AccentRedBrush}"/>
                                                    <Setter TargetName="icon" Property="Stroke" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
            
            <!-- Content -->
            <Grid Grid.Row="1" Margin="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Material List -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Material List" Style="{StaticResource TextLabel}" Margin="0,0,0,8"/>
                    
                    <Border Grid.Row="1" Background="{StaticResource BgBaseBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6">
                        <ListBox x:Name="MaterialList" 
                                 Background="Transparent" 
                                 BorderThickness="0"
                                 SelectionChanged="MaterialList_SelectionChanged">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem" BasedOn="{StaticResource ListBoxItemModern}"/>
                            </ListBox.ItemContainerStyle>
                            
                            <!-- Sample Items -->
                            <ListBoxItem Content="Hardwood" IsSelected="True"/>
                            <ListBoxItem Content="Softwood"/>
                            <ListBoxItem Content="Plywood"/>
                            <ListBoxItem Content="MDF"/>
                            <ListBoxItem Content="Chipboard"/>
                            <ListBoxItem Content="Acrylic"/>
                            <ListBoxItem Content="PVC"/>
                            <ListBoxItem Content="ABS"/>
                            <ListBoxItem Content="Carbon Fiber"/>
                        </ListBox>
                    </Border>
                </Grid>
                
                <!-- List Actions -->
                <StackPanel Grid.Column="2" VerticalAlignment="Top" Margin="0,24,0,0">
                    <Button Content="Add" Style="{StaticResource BtnSecondary}" Margin="0,0,0,8"/>
                    <Button Content="Duplicate" Style="{StaticResource BtnSecondary}" Margin="0,0,0,8"/>
                    <Button Content="Edit" Style="{StaticResource BtnSecondary}" Margin="0,0,0,8"/>
                    <Button Content="Delete" Style="{StaticResource BtnSecondary}" Margin="0,0,0,24"/>
                    <Button Content="Move Up" Style="{StaticResource BtnGhost}" Margin="0,0,0,4"/>
                    <Button Content="Move Down" Style="{StaticResource BtnGhost}" Margin="0,0,0,24"/>
                    <Button Content="Restore" Style="{StaticResource BtnGhost}"/>
                </StackPanel>
                
                <!-- Material Details -->
                <Grid Grid.Column="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Material Info" Style="{StaticResource TextLabel}" Margin="0,0,0,8"/>
                    
                    <Border Grid.Row="1" Background="{StaticResource BgBaseBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="16">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="16"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="16"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Material Name -->
                            <StackPanel Grid.Row="0">
                                <TextBlock Text="Material Name" Style="{StaticResource TextLabel}" Margin="0,0,0,6"/>
                                <TextBox x:Name="TxtMaterialName" Text="Hardwood" Style="{StaticResource InputText}"/>
                            </StackPanel>
                            
                            <!-- Material Image -->
                            <Grid Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="100"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Material Image" Style="{StaticResource TextLabel}" Margin="0,0,0,6"/>
                                    <Border Background="{StaticResource BgBaseBrush}" Height="80" CornerRadius="6" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                        <Grid>
                                            <TextBlock Text="No Image" Foreground="{StaticResource TextMutedBrush}" 
                                                       HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11"/>
                                        </Grid>
                                    </Border>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Bottom">
                                    <Button Content="Import Image" Style="{StaticResource BtnGhost}" Padding="8,6" FontSize="10" Margin="0,0,0,8"/>
                                    <Button Content="Apply" Style="{StaticResource BtnSecondary}" Padding="8,6" FontSize="10"/>
                                </StackPanel>
                            </Grid>
                            
                            <!-- Cutting Parameters -->
                            <StackPanel Grid.Row="4">
                                <TextBlock Text="DEFAULT CUTTING PARAMETERS" Style="{StaticResource TextLabel}" Margin="0,0,0,12"/>
                                
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="8"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="8"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <StackPanel Grid.Row="0" Grid.Column="0">
                                        <TextBlock Text="Feed Rate (mm/min)" Foreground="{StaticResource TextMidBrush}" FontSize="11" Margin="0,0,0,4"/>
                                        <TextBox Text="1000" Style="{StaticResource InputNumber}"/>
                                    </StackPanel>
                                    
                                    <StackPanel Grid.Row="0" Grid.Column="2">
                                        <TextBlock Text="Plunge Rate (mm/min)" Foreground="{StaticResource TextMidBrush}" FontSize="11" Margin="0,0,0,4"/>
                                        <TextBox Text="300" Style="{StaticResource InputNumber}"/>
                                    </StackPanel>
                                    
                                    <StackPanel Grid.Row="2" Grid.Column="0">
                                        <TextBlock Text="Spindle RPM" Foreground="{StaticResource TextMidBrush}" FontSize="11" Margin="0,0,0,4"/>
                                        <TextBox Text="18000" Style="{StaticResource InputNumber}"/>
                                    </StackPanel>
                                    
                                    <StackPanel Grid.Row="2" Grid.Column="2">
                                        <TextBlock Text="Depth per Pass (mm)" Foreground="{StaticResource TextMidBrush}" FontSize="11" Margin="0,0,0,4"/>
                                        <TextBox Text="2.0" Style="{StaticResource InputNumber}"/>
                                    </StackPanel>
                                    
                                    <StackPanel Grid.Row="4" Grid.Column="0">
                                        <TextBlock Text="Step Over (%)" Foreground="{StaticResource TextMidBrush}" FontSize="11" Margin="0,0,0,4"/>
                                        <TextBox Text="40" Style="{StaticResource InputNumber}"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
            
            <!-- Footer Buttons -->
            <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderBrush}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="16,0">
                    <Button Content="Close" Style="{StaticResource BtnSecondary}" Width="100" Click="Close_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
